<Window x:Class="WPF_01.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_01"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <!-- 
        XAML  is a EXtensible Application Markup Langage
    -->
    
    <!-- Adding two numbers -->
    <StackPanel>
        <Label Content="Num1" FontSize="25" ></Label>
        <TextBox Height="50" Width="200" Text="{Binding Path=Num1, Mode=TwoWay  }" ></TextBox>
        <Label Content="Num2" FontSize="25" ></Label>
        <TextBox Height="50" Width="200" Text="{Binding Path=Num2, Mode=TwoWay}" ></TextBox>
        <Label Content="Num3" FontSize="25" ></Label>
        <TextBox Height="50" Width="200" Text="{Binding Path=Result,Mode=TwoWay}" ></TextBox>



    </StackPanel>

 


</Window>
